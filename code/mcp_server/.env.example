# .env.example for mCP Server configuration

# API Keys for authentication (comma-separated if multiple)
# This is a mandatory setting. The application will not start if not set.
# Example: VALID_API_KEYS="your-secret-key-1,your-secret-key-2"
VALID_API_KEYS="your-secret-key"

# Redis connection settings
# REDIS_HOST="localhost"
# REDIS_PORT=6379

# Neural Engine Backend (e.g., "openai", "local")
# NEURAL_ENGINE_BACKEND="openai"

# OpenAI API Key (if NEURAL_ENGINE_BACKEND is "openai")
# OPENAI_API_KEY="your-openai-api-key"

# Local Model Path (if NEURAL_ENGINE_BACKEND is "local")
# LOCAL_MODEL_PATH="models/llama-3.1-8b.gguf"

# Serper API Key for web search (if used)
# SERPER_API_KEY="your-serper-api-key"